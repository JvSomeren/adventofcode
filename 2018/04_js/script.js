const src = document.body.innerText.split("\n").filter(x => x != "");

/**
 * Parse input
 */
for(let record of src) {
  console.log(record)
}

/**
 * Part one
 */


console.log("Part one:", undefined);
